import theme from "styles/theme";

export const blackBorder = `1px solid ${theme.light.foreground}`;
export const whiteBorder = `1px solid ${theme.light.background}`;
export const transparentBorder = `1px solid transparent`;
